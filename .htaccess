<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward all requests to the public directory unless already there
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>


